#계산기 만들기
#클래스를 이용해서 계산 메서드만듬
#초깃값 입력을 위해서 생성자 기능 입력함
#클래스 상속을 이용해서 추가기능이 들어간 새로운 계산기 만듬
#메서드오버라이딩, 클래스변수 등 추가 학습함


class Fourcal:
    def __init__(self, first, second):
        self.first = first
        self.second = second
    def setdata(self,first,second):
        self.first = first
        self.second = second
    def add(self):
        result = self.first + self.second
        return result 
    def mul(self):
        result = self.first * self.second
        return result 
    def sub(self):
        result = self.first - self.second
        return result 
    def div(self):
        result = self.first / self.second
        return result 


class MoreFourcal(Fourcal):
    def pow(self):
        result = self.first ** self.second
        return result
